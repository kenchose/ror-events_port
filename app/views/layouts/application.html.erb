<!DOCTYPE html>
<html>
<head>
  <title>Events</title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/js/bootstrap.min.js" integrity="sha384-ChfqqxuZUCnJSK3+MXmPNIyE6ZbWh2IMqE241rYiqJxyMiZ6OW/JmZQ5stwEULTy" crossorigin="anonymous"></script>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
<header>
  <div class="container"  id="home">
    <nav class="navbar navbar-expand-lg fixed-top">
      <div class="brand_button pull:left">
        <button class="btn btn-outline-info btn-large " disabled>Events</button>
      </div>
      <button class="btn navbar-toggler" data-toggle="collapse" data-target="#navbarItems">
        <span class="navbar-toggler-icon"><i class="fas fa-bars"></i></span>                
      </button>                        
      <div class="collapse navbar-collapse justify-content-end" id="navbarItems">
        <ul class="navbar-nav">
          <% if current_user %>                            
          <li class="nav-item"><a class="nav-link d-inline scroll" href="/events" >Event Dashboard</a></li>  
          <li class="nav-item"><a class="nav-link d-inline scroll" href="users/<%= current_user.id %>/edit">Edit Profile</a></li>
          <li class="nav-item">
            <form action="/sessions/<%=current_user.id%>" method="post" class="nav-link d-inline scroll">
              <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
              <input type="hidden" name="_method" value="delete">
              <input style="float: right" class="submitLink" type="submit" value="Log Out">
            </form>
          </li>
          <% else %>
            <button class="btn btn-outline-info nav-link"><a class="new-link d-inline" style="text-decoration: none; color:white" href="/sessions/new">Log In</a></button> 
          <% end %>
        </ul> 
      </div>
    </nav>
  </div>
</header>

<% if new_user_path %>
  <h1 class="display 3 text-center">Plan and Discorever</h1>
<% end %>
<% if flash[:errors] %>
  <% flash[:errors].each do |e| %>
    <p><%= e %></p>
  <% end %>
<% end %>

<% if flash[:notice] %>
  <% flash[:notice].each do |n| %>
    <p><%= n %></p>
  <% end %>
<% end %>

  <!-- <% if current_user %>
    <form action="/sessions/<%=current_user.id%>" method="post">
      <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
      <input type="hidden" name="_method" value="delete">
      <button style="float: right" class="btn btn-secondary" type="submit">Log Out</button>
    </form>
  <% end %> -->
<!-- <div class="container" style=" display:block;">
  <% if current_user %>
    <div class="container">
      <div class="row">
        <div class="col text-right">
          <form action="/sessions/<%=current_user.id%>" method="post">
            <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
            <input type="hidden" name="_method" value="delete">
            <button class="btn btn-secondary" type="submit">Log Out</button>
          </form>
        </div>
      </div>
    </div>
  <% end %>
</div> -->
<%= yield %>

</body>
</html>
